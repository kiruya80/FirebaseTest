apply plugin: 'com.android.application'
//apply plugin: 'com.android.library'

android {
    signingConfigs {
        release {
//            storeFile file('/Users/KILHO/Dropbox/iullim/ullim_appstore/Keystore/ullimkey.keystore')
//            storePassword 'ullimkey'
        }
        debug {
//            storeFile file('/Users/KILHO/Dropbox/iullim/ullim_appstore/Keystore/ullim_debugkey.keystore')
            keyAlias 'androiddebugkey'
            keyPassword 'android'
            storeFile file('/Users/KILHO/debug.keystore')
            storePassword 'android'

//            def Properties props = new Properties()
//            def propFile = new File('gradle.properties')
//            if (propFile.canRead())
//                props.load(new FileInputStream(propFile))
//            debug {
//                storeFile file(props['debug_keystore'])
//                storePassword props['debug_keystore_pass']
//                keyAlias props['debug_key_alias']
//                keyPassword props['debug_key_pass']
//            }
        }
    }
    compileSdkVersion 25
    buildToolsVersion '25.0.0'
    defaultConfig {
        applicationId "com.ulling.firebasetest"
        minSdkVersion 16
        targetSdkVersion 25
        versionCode 1
        versionName "1.0"
        multiDexEnabled true
    }
    buildTypes {
        release {
            minifyEnabled true
//            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
//    compile 'com.github.kiruya80:UllingCoreLib:0.0.12'
    compile 'com.android.support:appcompat-v7:25.0.0'
    compile 'com.android.support:design:25.0.0'
    compile 'com.github.bumptech.glide:glide:3.6.1'
    //    compile 'de.hdodenhof:circleimageview:1.3.0'
    //    compile 'com.github.jitpack-io:android-jitpack-library-example:1.5'
    // Google
    compile 'com.google.android.gms:play-services-auth:9.8.0'
    // Firebase
    compile 'com.google.firebase:firebase-database:9.8.0'
    compile 'com.google.firebase:firebase-auth:9.8.0'
    compile 'com.google.firebase:firebase-config:9.8.0'
    compile 'com.google.firebase:firebase-messaging:9.8.0'
    compile 'com.google.firebase:firebase-crash:9.8.0'
    // Firebase UI
    compile 'com.firebaseui:firebase-ui-database:0.4.0'
    //    compile 'com.google.android.gms:play-services-ads:9.8.0'
    //    compile 'com.google.android.gms:play-services-appinvite:9.8.0'

    // Firebase UI
    // Used in FirebaseUIActivity. The FirebaseUI-Android library has a minSdk level of 16
    // so this library is only included in the minSdkJellybean variant.
//    minSdkJellybeanCompile 'com.firebaseui:firebase-ui-auth:0.6.2'

    // Facebook Android SDK (only required for Facebook Login)
    // Used in FacebookLoginActivity. This is only compiled into the minSdkJellybean variant of this
    // app. You can build a 'minSdkGingerbread' variant to test on devices with 9 <= SDK <= 15.
//    minSdkJellybeanCompile 'com.facebook.android:facebook-android-sdk:4.9.0'
    compile 'com.facebook.android:facebook-android-sdk:4.17.0'

    // Twitter Android SDK (only required for Twitter Login)
    compile('com.twitter.sdk.android:twitter-core:1.7.0@aar') {
        transitive = true;
    }
    compile('com.twitter.sdk.android:twitter:1.7.0@aar') {
        transitive = true;
    }
}
// ADD THIS AT THE BOTTOM
apply plugin: 'com.google.gms.google-services'
